# 配置中心 3.4 集成设计

> 编制日期：2026-03-04
> 目标：将当前独立弹窗 `ConfigCenterView` 集成到主窗体 `SettingsView` 中，消除模态对话框交互。

---

## 1. 现状分析

### 1.1 当前架构

| 组件 | 形式 | 行数 | 说明 |
|------|------|------|------|
| `ConfigCenterView.axaml` | 独立 Window（模态对话框） | 621 行 | 包含 9 个 TabItem，左侧 Tab 导航 |
| `ConfigCenterView.axaml.cs` | 代码后置（事件驱动） | 1,317 行 | 大量手动控件操作、无 ViewModel |
| `SettingsView.axaml` | 主窗体内嵌 UserControl | 91 行 | 简易仪表板，含"打开配置中心"按钮 |

### 1.2 ConfigCenter 9 个 Tab

| # | Header | 内容概述 |
|---|--------|----------|
| 1 | 订阅与鉴权 | Azure 订阅 CRUD、密钥管理、区域选择、连接测试 |
| 2 | 录音与存储 | 录音格式、Azure Storage Blob 配置与验证 |
| 3 | AI 基础配置 | AI Provider 选择、API Key/Endpoint、AAD 认证 |
| 4 | 输入与识别 | 语音识别参数（语言、置信度、超时） |
| 5 | 字幕与文本 | 实时文本长度、历史条目数、字号 |
| 6 | 洞察 | AI 洞察预设按钮、系统提示词 |
| 7 | 复盘 | 复盘 Sheet 预设与自定义提示词 |
| 8 | Media Studio | 图片/视频生成 AI 配置（独立 Provider） |
| 9 | 实时翻译字号 | 默认字号选择 |

### 1.3 问题

1. **模态对话框阻断工作流** — 打开配置中心后无法同时操作翻译/复盘
2. **代码后置过重** — 1,317 行事件驱动代码，无 ViewModel，不易维护
3. **与 SettingsView 重复** — 订阅选择、设备管理在两处分别实现
4. **保存语义不清晰** — 用户必须点"保存"按钮，否则修改丢失

---

## 2. 目标设计

### 2.1 总体方案

将 ConfigCenter 所有功能直接嵌入 `SettingsView`，取消弹窗。SettingsView 采用 **左侧锚点导航 + 右侧瀑布流** 布局（ScrollSpy 模式），类似迅雷设置页的交互风格。

### 2.2 SettingsView 新布局

```
SettingsView
┌──────────────────────────────────────────────────────────┐
│  ┌──左侧导航 (固定)──┐  ┌──右侧内容 (ScrollViewer)──┐  │
│  │                     │  │                             │  │
│  │  ● 订阅管理 ←active │  │  ═ 订阅管理 ═══════════    │  │
│  │    音频设备          │  │    订阅选择 / CRUD / 测试   │  │
│  │    录音与存储        │  │                             │  │
│  │    AI 基础配置       │  │  ═ 音频设备 ═══════════    │  │
│  │    语音识别          │  │    输入/输出设备选择        │  │
│  │    字幕与文本        │  │                             │  │
│  │    AI 洞察预设       │  │  ═ 录音与存储 ═════════    │  │
│  │    复盘预设          │  │    格式 / Blob 配置        │  │
│  │    Media Studio      │  │                             │  │
│  │    外部链接          │  │  ═ AI 基础配置 ════════    │  │
│  │    帮助与关于        │  │    Provider / Key / AAD    │  │
│  │                     │  │                             │  │
│  │                     │  │    ... 后续分区瀑布流 ...   │  │
│  │                     │  │             ▼               │  │
│  └─────────────────────┘  └─────────────────────────────┘  │
│  ┌─状态栏──────────────────────────────────────────────┐   │
│  │  ✓ 配置已自动保存                                    │   │
│  └─────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

### 2.3 右侧分区列表

右侧所有分区平铺展示，无折叠，用户可直接浏览或通过左侧导航跳转：

| # | 分区名 | 内容来源 |
|---|--------|----------|
| 1 | 订阅管理 | 当前订阅选择（合并 SettingsView）+ ConfigCenter Tab 1 |
| 2 | 音频设备 | 保留现有 SettingsView 功能 |
| 3 | 录音与存储 | ConfigCenter Tab 2 |
| 4 | AI 基础配置 | ConfigCenter Tab 3 |
| 5 | 语音识别 | ConfigCenter Tab 4 |
| 6 | 字幕与文本 | ConfigCenter Tab 5 + Tab 9（字号合并） |
| 7 | AI 洞察预设 | ConfigCenter Tab 6 |
| 8 | 复盘预设 | ConfigCenter Tab 7 |
| 9 | Media Studio | ConfigCenter Tab 8 |
| 10 | 外部链接 | 保留现有 SettingsView 功能 |
| 11 | 帮助与关于 | 保留现有 SettingsView 功能 |

### 2.4 左侧导航交互规则

- **点击跳转**：点击左侧导航项 → 右侧 `ScrollIntoView()` 滚动到对应分区
- **滚动高亮**：右侧滚动时 → 检测当前可见分区 → 自动高亮左侧对应导航项
- **固定不滚动**：左侧导航栏随右侧内容固定，不随右侧滚动
- **视觉样式**：当前活动项使用蓝色高亮条（类似迅雷设置页效果）

### 2.5 分区卡片组件设计

每个分区使用 `Border` + `TextBlock` 标题，不使用 Expander（全部平铺展示）：

```xml
<Border Name="Section_Subscription"
        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
        BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,16">
    <StackPanel Spacing="12">
        <TextBlock Text="订阅管理" Theme="{StaticResource SubtitleTextBlockStyle}" />
        <Separator />
        <!-- 分区内容 -->
    </StackPanel>
</Border>
```

### 2.6 XAML 结构草案

```xml
<Grid ColumnDefinitions="180,*">
    <!-- 左侧锚点导航 -->
    <ListBox Grid.Column="0"
             SelectedItem="{Binding ActiveSection}"
             SelectionChanged="OnNavSectionChanged"
             Classes="settings-nav">
        <ListBoxItem Tag="Section_Subscription" Content="订阅管理" />
        <ListBoxItem Tag="Section_Audio" Content="音频设备" />
        <ListBoxItem Tag="Section_Storage" Content="录音与存储" />
        <!-- ... -->
    </ListBox>

    <!-- 右侧瀑布流内容 -->
    <ScrollViewer Grid.Column="1" Name="SettingsScroller"
                  ScrollChanged="OnSettingsScrollChanged">
        <StackPanel Spacing="16" Margin="24,0">
            <Border Name="Section_Subscription"> ... </Border>
            <Border Name="Section_Audio"> ... </Border>
            <Border Name="Section_Storage"> ... </Border>
            <!-- 后续分区 -->
        </StackPanel>
    </ScrollViewer>
</Grid>
```

### 2.7 ScrollSpy 实现要点

```csharp
// 点击左侧 → 滚动到目标分区
private void OnNavSectionChanged(object sender, SelectionChangedEventArgs e)
{
    var tag = (e.AddedItems[0] as ListBoxItem)?.Tag as string;
    var target = this.FindControl<Border>(tag);
    target?.BringIntoView();
}

// 右侧滚动 → 更新左侧高亮
private void OnSettingsScrollChanged(object sender, ScrollChangedEventArgs e)
{
    // 遍历各分区 Border，找到当前可见区域内最靠上的分区
    // 更新 ListBox.SelectedItem（需抑制反向触发滚动）
}
```

### 2.8 保存策略

- **即时保存**：每个字段变更后自动保存（通过 debounce 防抖）
- 移除全局"保存"按钮
- 保留"测试连接"等验证按钮
- 状态栏显示"配置已自动保存"提示

---

## 3. ViewModel 改造

### 3.1 新增 `SettingsViewModel`

从 `ConfigCenterView.axaml.cs` 的 1,317 行事件驱动代码中，提取业务逻辑到 `SettingsViewModel`：

```
SettingsViewModel
├── SubscriptionManagement（订阅 CRUD + 验证）
├── StorageConfig（录音与 Blob 存储）
├── AiProviderConfig（AI 基础配置）
├── RecognitionConfig（输入与识别参数）
├── TextDisplayConfig（字幕与文本 + 字号）
├── InsightPresetsConfig（洞察预设管理）
├── ReviewPresetsConfig（复盘预设管理）
├── MediaStudioConfig（Media Studio AI 配置）
└── AutoSave（防抖自动保存逻辑）
```

### 3.2 DI 注册

```csharp
services.AddSingleton<SettingsViewModel>();
```

### 3.3 与 ConfigViewModel 关系

- `ConfigViewModel` 保留为轻量运行时订阅状态管理（选中订阅、语言切换）
- `SettingsViewModel` 管理完整配置编辑（CRUD、验证、高级参数）
- 两者共享同一 `AzureSpeechConfig` 实例

---

## 4. 实施步骤

### Step 1：创建 SettingsViewModel（预估 2h）

- 从 ConfigCenterView.axaml.cs 提取逻辑
- 实现 ICommand 属性替代事件处理器
- 实现自动保存防抖

### Step 2：重写 SettingsView.axaml（预估 3h）

- 按 §2.2 结构排列卡片
- 使用 MVVM 绑定替代代码后置
- 保持与现有 ConfigCenterView 相同的功能覆盖

### Step 3：迁移验证逻辑（预估 1h）

- 订阅连接测试
- AI 配置测试
- Storage 验证

### Step 4：移除 ConfigCenterView（预估 0.5h）

- 删除 `ConfigCenterView.axaml` + `.cs`
- 删除 `ShowConfigCommand` 中的弹窗逻辑
- 更新导航——Settings 页直接展示全部配置

### Step 5：回归测试（预估 1h）

- 验证所有 9 个配置区域功能正常
- 验证即时保存与配置文件一致性
- 验证与其他页面的联动（翻译按钮状态、设备刷新等）

---

## 5. 风险与注意事项

| 风险 | 缓解措施 |
|------|----------|
| SettingsView 内容过多导致滚动过长 | 使用 Expander 默认折叠非常用区域 |
| 自动保存频繁写磁盘 | 使用 500ms debounce + dirty flag |
| ConfigCenterView 代码后置复杂 | 分步提取，每步验证构建 |
| 已有 ConfigCenter 测试流程失效 | 保持功能等价，逐项对照验证 |

---

## 6. 不在范围

- Media Studio 配置不与 Media Studio 视图合并（它们职责不同）
- AI Config View（`AiConfigView.axaml`）保持独立——它是洞察面板的入口
- 本次仅设计，不实施代码变更

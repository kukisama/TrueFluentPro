import React from "react";
import ReactDOM from "react-dom/client";
import { FloatingSubtitleApp } from "./views/FloatingSubtitleWindow";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FloatingSubtitleApp />
  </React.StrictMode>
);
